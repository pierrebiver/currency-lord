query countrySelect($isoCode: String) {
    countryByIso(isoCode: $isoCode){
        name
        alpha2Code
        currencies {
            code
            name
            symbol
        }
        capital
        population
    }
}